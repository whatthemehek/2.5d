import { render, screen, fireEvent, cleanup } from '@testing-library/react'
import { afterEach, beforeEach, describe, expect, it } from 'vitest'
import { App } from '../app/App'
import { useUIStore } from '../state/uiStore'
afterEach(cleanup)
beforeEach(() => useUIStore.setState({ mode: 'compose', tab: 'layers', tool: 'Select', selected: 'left-arm', dialog: null, time: 4.2, playing: false, grid: true, safeFrame: true, material: 'White paper', expanded: { character: true, transform: true, layer: true, motion: false, edge: false, material: false, shadow: false }, keyframes: [] }))
describe('Papercut wireframe', () => { it('switches workspace modes', () => { render(<App/>); fireEvent.click(screen.getByText('Stage')); expect(useUIStore.getState().mode).toBe('stage') }); it('selects a tool and object', () => { render(<App/>); fireEvent.click(screen.getByText('tools')); fireEvent.click(screen.getByText('Rotate')); expect(useUIStore.getState().tool).toBe('Rotate'); fireEvent.click(screen.getByText('layers')); fireEvent.click(screen.getAllByText('Right Arm')[0]); expect(useUIStore.getState().selected).toBe('right-arm') }); it('opens and closes dialogs', () => { render(<App/>); fireEvent.click(screen.getByText('Preview')); expect(screen.getByRole('dialog')).toBeInTheDocument(); fireEvent.click(screen.getByLabelText('Close dialog')); expect(screen.queryByRole('dialog')).not.toBeInTheDocument() }); it('updates playhead time', () => { render(<App/>); const ruler = document.querySelector('.ruler-area')!; Object.defineProperty(ruler, 'getBoundingClientRect', { value: () => ({ left: 0, width: 120 }) }); fireEvent.click(ruler, { clientX: 60 }); expect(useUIStore.getState().time).toBe(6) }); it('keeps the three layer limit disabled', () => { render(<App/>); expect(screen.getByText('Add Layer').closest('button')).toBeDisabled() }) })
