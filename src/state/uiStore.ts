import { create } from 'zustand'
export type Mode = 'compose' | 'stage'
export type Tab = 'layers' | 'assets' | 'tools'
export type DialogName = 'new' | 'import' | 'preview' | 'export' | 'shortcuts' | 'delete' | null
type UIState = { mode: Mode; tab: Tab; tool: string; selected: string; dialog: DialogName; time: number; playing: boolean; grid: boolean; safeFrame: boolean; material: string; expanded: Record<string, boolean>; keyframes: string[]; setMode: (mode: Mode) => void; setTab: (tab: Tab) => void; setTool: (tool: string) => void; select: (id: string) => void; setDialog: (dialog: DialogName) => void; setTime: (time: number) => void; togglePlaying: () => void; toggleGrid: () => void; toggleSafe: () => void; setMaterial: (material: string) => void; toggleExpanded: (id: string) => void; toggleKey: (id: string) => void }
export const useUIStore = create<UIState>((set) => ({ mode: 'compose', tab: 'layers', tool: 'Select', selected: 'left-arm', dialog: null, time: 4.2, playing: false, grid: true, safeFrame: true, material: 'White paper', expanded: { character: true, transform: true, layer: true, motion: false, edge: false, material: false, shadow: false }, keyframes: [], setMode: (mode) => set({ mode }), setTab: (tab) => set({ tab }), setTool: (tool) => set({ tool }), select: (selected) => set({ selected }), setDialog: (dialog) => set({ dialog }), setTime: (time) => set({ time: Math.max(0, Math.min(12, time)) }), togglePlaying: () => set((s) => ({ playing: !s.playing })), toggleGrid: () => set((s) => ({ grid: !s.grid })), toggleSafe: () => set((s) => ({ safeFrame: !s.safeFrame })), setMaterial: (material) => set({ material }), toggleExpanded: (id) => set((s) => ({ expanded: { ...s.expanded, [id]: !s.expanded[id] } })), toggleKey: (id) => set((s) => ({ keyframes: s.keyframes.includes(id) ? s.keyframes.filter((key) => key !== id) : [...s.keyframes, id] })) }))
