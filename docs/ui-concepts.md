# UI concepts

Papercut follows desktop creative-tool conventions: a compact global toolbar, persistent layer/assets/tools sidebar, artwork-first viewport, property inspector, and timeline. Compose is a front-facing object and layer view; Sketch isolates one frozen layer for vector drawing while dimming its context layers; Stage visually implies nonuniform depth with CSS perspective only. Warm paper artwork intentionally contrasts with the dark neutral editor chrome.
